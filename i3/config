# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

font pango:DejaVuSansMono, 10


# start-up initial apps
# exec --no-startup-id compton -b --config ~/.compton.conf
# exec --no-startup-id udiskie --no-tray
# exec --no-startup-id unclutter -idle 3
# exec --no-startup-id dunst
# exec --no-startup-id /home/tim/bin/low-battery-check-alert
# exec --no-startup-id nm-applet --sm-disable # NetworkManager, if not using connman
# exec --no-startup-id xflux -l 51 -k 3400
# exec --no-startup-id redshift-gtk -l 51.336:0.267 -t 6500:3500 -m randr:preserve=1
# exec --no-startup-id copyq

exec --no-startup-id /home/bulentk/.config/i3/bin/sxhkd.sh
exec --no-startup-id /home/bulentk/.config/i3/bin/polybar.sh

exec --no-startup-id thunderbird
exec --no-startup-id Discord

set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7 "
set $ws8 "8 "
set $ws9 "9 "

# <class>               border background text  indicator  child_border

client.focused          #6d8895 #F0C674  #000000 #fb0009   #F0C674
client.focused_inactive #6d8895 #004052  #FFFFFF #fb0009   #6d8895
client.unfocused        #6d8895 #004052  #FFFFFF #fb0009   #6d8895
client.urgent           #6d8895 #004052  #FFFFFF #fb0009   #6d8895
client.placeholder      #6d8895 #004052  #FFFFFF #fb0009   #6d8895


# hide borders when we have only one window

hide_edge_borders smart

# Assign windows to their respective workspaces xprop=> class

assign [class="Thunderbird"]         $ws7
assign [class="vlc"]                 $ws9
assign [class="rhythmbox|Rhythmbox"] $ws9
for_window [instance="spotify|Spotify"] move to workspace $ws9
assign [class="Skype|skypeforlinux"] $ws8
assign [class="discord"]             $ws8
assign [class="slack|Slack"]         $ws8

floating_modifier $mod

# disable title bars on new windows (not floats)
new_window pixel 2
new_float normal

# floats
for_window [class="feh"] floating enable
for_window [class="feh"] border normal
for_window [class=".blueman-manager-wrapped"] floating enable
for_window [class=".blueman-manager-wrapped"] border normal
for_window [class="pavucontrol|Pavucontrol"] floating enable
for_window [class="pavucontrol|Pavucontrol"] border normal


# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"
